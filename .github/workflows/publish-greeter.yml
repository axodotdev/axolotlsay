name: Greeter

on:
  workflow_call:
    inputs:
      plan:
        required: true
        type: string

jobs:
  greeter:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: |
          echo "Hello!"
          echo "Plan is: ${{ inputs.plan }}"
